<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.homeaidkit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".passwordEdit"></activity>
        <activity android:name=".emailEdit" />
        <activity android:name=".account" />
        <activity android:name=".editDrug" />
        <activity android:name=".addDrug" />
        <activity android:name=".modifyDeletecategory" />
        <activity android:name=".addCategory" />
        <activity android:name=".categories" />
        <activity android:name=".drugModify" />
        <activity android:name=".register" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>